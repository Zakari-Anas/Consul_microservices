server:
  port: 8888
spring:
  spring:
    cloud:
      consul:
        host: ${SPRING_CLOUD_CONSUL_HOST:localhost}
        port: ${SPRING_CLOUD_CONSUL_PORT:8500}
        discovery: gateway-service
  application:
    name: gateway-service
  cloud:
    discovery:
      enabled: true
